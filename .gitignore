# Logs
logs
*.log
npm-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Env — never commit secrets
.env
.env.*
!.env.example

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supabase local
supabase/.branches
supabase/.temp

# Installed skill packs (local tooling, not app code)
.agents/

# Installed marketing skill packs (local tooling, not app code)
.claude/skills/
skills-lock.json

# Local dev helper (machine-specific path)
start-dev.bat

# Raw + compressed portfolio videos (uploaded to Supabase bucket, not git)
public/raw/

.superpowers/

# Design-system sync tooling (local only)
.ds-sync/
.design-sync/
